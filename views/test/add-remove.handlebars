{{! This is the partial handlebar responsible for the add/remove functions. We are using tailwind CSS for the styling. }}

<form action="/filmadded" method="POST" class="mx-auto w-1/2">
  <div class="flex justify-center mt-2 mb-3">
    <div class="flex justify-center w-7 relative">
      <input type="hidden" name="movieName" value="{{movieName}}" />
      {{!-- This is a conditional statement ({{#if (isInWatchlist movieName watchlist)}}) checks if the movie is already in the watchlist. --}}
      {{#if (isInWatchlist movieName watchlist)}}
        <button type="submit" class="relative group">
          <img src="/images/remove.jpg" alt="subtract button" class="w-7" />
          <span
            class="absolute bg-black text-white px-2 py-1 text-xs rounded-md bottom-0 right-full transform translate-y-1/4 opacity-0 group-hover:opacity-100 transition-opacity duration-300"
          >
            Remove from Watchlist
          </span>
        </button>
      {{else}}
        <button type="submit" class="relative group">
          <img src="/images/add.jpg" alt="" class="w-7" />
          <span
            class="absolute bg-black text-white px-2 py-1 text-xs rounded-md bottom-0 right-full transform translate-y-1/4 opacity-0 group-hover:opacity-100 transition-opacity duration-300"
          >
            Add to Watchlist
          </span>
        </button>
      {{/if}}
    </div>
  </div>
</form>