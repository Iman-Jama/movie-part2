{{!-- This is the handlebar responsible for the filmlsit page. We are using tailwind CSS for the styling.--}}

<div class="flex justify-center bg-zinc-200 h-auto">
    <div class="flex flex-col items-center">
        <h2 class="mb-20 mt-12 text-3xl">
            Watch list
        </h2>
        <ul>
{{!-- The 'each' is a helper block which loops over the watchlistData array and assigns each item to the variable movie --}}
  {{#each watchlistData as |movie|}}
  <li class="mb-20 p place-content-center border-b border-gray-300 pb-5">
    <h3 class="text-center mb-5 text-xl">
      <strong>
{{!-- This renders a hyperlink which is generated by encodedURI.
Encdoded URI changes the search into a format the URL understands, e.g toy story would be encoded to toy-story. This gets changed back when the data is pulled back to the front end.  --}}
        <a href="/film/{{encodeURI movie.movie_name}}">{{movie.movie_name}}</a>
      </strong>
    </h3>
    <div class="flex justify-between mb-10">
      <a href="/film/{{encodeURI movie.movie_name}}">
{{!-- The movie.poster.url is pulling the url that is saved to the database once the api call has been successful.  --}}
        <img class="ml-10 h-64 mb-5" src="{{movie.poster_url}}" />
      </a>
      <p class="ml-10 mr-10 text-center mt-14">{{movie.description}}</p>
      <form action="/filmremoved" method="POST">
        <input type="hidden" name="movie_name" value="{{movie.movie_name}}">
        <button type="submit" class="mr-10 bg-amber-400 hover:bg-slate-400 text-black font-bold py-1 px-2 rounded-full h-10 mt-20">Remove</button>
      </form>
    </div>
  </li>
  {{/each}}
</ul>

    </div>
</div>